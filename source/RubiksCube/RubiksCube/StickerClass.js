/*class Sticker {
    constructor( mat ){
        if (mat == 'y')    {this.color = new THREE.MeshPhongMaterial( {      color: 0xffffff, 
                                                                            side: THREE.DoubleSide, 
                                                                            map: yellowSticker} ) };
        
        if (mat == 'w')    {this.color = new THREE.MeshPhongMaterial( {      color: 0xffffff, 
                                                                            side: THREE.DoubleSide, 
                                                                            map: whiteSticker} ) };

        if (mat == 'r')    {this.color = new THREE.MeshPhongMaterial( {      color: 0xffffff, 
                                                                            side: THREE.DoubleSide,  
                                                                            map: redSticker} ) };

        if (mat == 'o')    {this.color = new THREE.MeshPhongMaterial( {      color: 0xffffff, 
                                                                            side: THREE.DoubleSide,                             
                                                                            map: orangeSticker} )};

        if (mat == 'g')    {this.color = new THREE.MeshPhongMaterial( {      color: 0xffffff, 
                                                                            side: THREE.DoubleSide, 
                                                                            map: greenSticker} )};

        if (mat == 'b')    {this.color = new THREE.MeshPhongMaterial( {      color: 0xffffff, 
                                                                            side: THREE.DoubleSide, 
                                                                            map: blueSticker} ) };
    }
}
*/
/*
class Sticker {
    constructor(mat) {
        if (mat === 'y') {
            this.mat = new THREE.MeshPhongMaterial({
                color: 0xffffff,
                side: THREE.DoubleSide,
                map: yellowSticker
            });
        }

        if (mat === 'w') {
            this.mat = new THREE.MeshPhongMaterial({
                color: 0xffffff,
                side: THREE.DoubleSide,
                map: whiteSticker
            });
        }

        if (mat === 'r') {
            this.mat = new THREE.MeshPhongMaterial({
                color: 0xffffff,
                side: THREE.DoubleSide,
                map: redSticker
            });
        }

        if (mat === 'o') {
            this.mat = new THREE.MeshPhongMaterial({
                color: 0xffffff,
                side: THREE.DoubleSide,
                map: orangeSticker
            });
        }

        if (mat === 'g') {
            this.mat = new THREE.MeshPhongMaterial({
                color: 0xffffff,
                side: THREE.DoubleSide,
                map: greenSticker
            });
        }

        if (mat === 'b') {
            this.mat = new THREE.MeshPhongMaterial({
                color: 0xffffff,
                side: THREE.DoubleSide,
                map: blueSticker
            });
        }
    }
}

var redSticker = new THREE.TextureLoader().load( "./colors/RED.png" );
var orangeSticker = new THREE.TextureLoader().load( "./colors/ORANGE.png" );
var yellowSticker = new THREE.TextureLoader().load( "./colors/YELLOW.png" );
var whiteSticker = new THREE.TextureLoader().load( "./colors/WHITE.png" );
var greenSticker = new THREE.TextureLoader().load( "./colors/GREEN.png" );
var blueSticker = new THREE.TextureLoader().load( "./colors/BLUE.png" );

var READY = false;
*/
